import Renderer from "./renderer.js";

class Camera{
    constructor(target, width, height){
        this.target = target;
        this.width = width;
        this.height = height;
        this.x = 0;
        this.y = 0;
}
update(){
    this.x = this.target.x + 
}
}